# mux compatibility package

`mux` is the legacy package and command name for [Xum](https://github.com/coder/xum).
It forwards to the matching `@coder/xum` release so existing scripts keep working during
the rename. New installations should use the `@coder/xum` package and `xum` command.
