/** Preferred size for a parent's direct bench of reusable standalone sub-agents. */
export const SUBAGENT_REUSABLE_BENCH_TARGET = 7;

/** Exclusive upper bound for that bench; intentional grouped runs are temporary exceptions. */
export const SUBAGENT_REUSABLE_BENCH_EXCLUSIVE_LIMIT = 10;
